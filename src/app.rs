//! Application lifecycle and shared runtime state: startup wiring, the shared
//! app state, window actions, and performance tracing.

pub mod actions;
pub mod startup;
pub mod state;
pub mod trace;
